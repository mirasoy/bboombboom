spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://127.0.0.1:3326/b4db
    username: mira
    password: Mira7838
  sql :
    init :
      mode : always
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    defer-datasource-initialization : true
    hibernate:
      ddl-auto: create
